version: 2
jobs:
  build:
    machine:
      image: circleci/classic:201711-01
    steps:
      - checkout
      - run: docker-compose -f ~/project/2/app.yml build
  api-test:
    docker:
      - image: circleci/node:8.11.1
    working_directory: ~/project/2/
    steps: 
      - checkout
      - run: pwd && ls
      - run: make test-api
    requires:
      - build
  chess-test:
    docker:
      - image: circleci/node:8.11.1
    working_directory: ~/project/2/
    steps: 
      - checkout
      - run: make test-chess
    requires:
      - build
  auth-test:
    docker:
      - image: circleci/node:8.11.1
    working_directory: ~/project/2/
    steps: 
      - checkout
      - run: make test-auth
    requires:
      - build

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - api-test
      - chess-test
      - auth-test