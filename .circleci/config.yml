version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.11.1
    steps:
      - checkout
      - run: ~/project/2/make build
  test:
    docker:
      - image: circleci/node:8.11.1
    steps:
      - checkout
      - run: pwd && ls
      - run: ~/project/2/make test

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test